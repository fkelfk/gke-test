kind: Service
apiVersion: v1
metadata:
  name: api_loadbalancer
spec:
  selector:
    run: api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer

---

kind: Service
apiVersion: v1
metadata:
  name: api_cluster
spec:
  selector:
    run: api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
